@page "/webring/<user id>/<blog name>"
@using Client.Pages.Webring.Jorson.Components

@if (_blogs is not null)
{
    <div id="body">
        <BackLink Link="/webring/<user id>" PageName="home" />
        <h1>Blog title</h1>
        <div>
            <ul>
                @foreach (var (blog, index) in _blogs.Select((item, index) => (item, index)))
                {
                    <li>
                        <h3>
                            <a href="@($"/webring/<user id>/<blog name>/{index}")">
                                @blog.Date.ToShortDateString() - @((MarkupString)blog.Title)
                            </a>
                        </h3>
                    </li>
                }
            </ul>
        </div>
    </div>
}